export default function BandInfo({ stage }) {
  console.log(stage);
  return (
    <section>
      BandInfo
      {/* {Object.values(stage).map((act, index) => (
        <div>
          {act.act}
          {stage[index].act}
        </div>
      ))}
      ;<p>{stage}</p> */}
    </section>
  );
}
